# Workflow Improvement

1. Validator phải phân biệt range validation và business threshold.
2. Calibration report cần versioned dataset và per-rule/action metrics.
3. UI không được dùng score color/copy như enforcement authority trước calibration.
