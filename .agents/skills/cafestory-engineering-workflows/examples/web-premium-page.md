# Example: Web Premium Page

User request:

```text
Add a premium subscription page in the CafeStory web app.
```

Skill workflow:

1. Read `references/web-workflow.md`.
2. Check `2-cafe-story-nextjs-web/AGENTS.md`.
3. Read relevant Next.js docs under `node_modules/next/dist/docs/` if available.
4. Inspect existing route, layout, API client, auth, and Tailwind patterns.
5. Implement the page with loading/error/unauthorized states if API-backed.
6. Validate with available package scripts.

Expected output:

- Web app code changes.
- Validation command results.
- Any backend/API assumptions called out explicitly.
