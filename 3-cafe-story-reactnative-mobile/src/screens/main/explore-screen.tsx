import { EmptyState, Screen } from "../../components";

export function ExploreScreen() {
  return (
    <Screen>
      <EmptyState
        description="Cafe discovery, search, and curated collections will live here."
        title="Explore cafes"
      />
    </Screen>
  );
}
